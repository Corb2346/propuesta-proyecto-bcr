import { ColorDinamicoDirective } from './color-dinamico.directive';

describe('ColorDinamicoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDinamicoDirective();
    expect(directive).toBeTruthy();
  });
});
